# Same as linear_selector.yaml, but with the deep_head ReLU head instead of the linear head
defaults:
  - base
  - _self_

wandb_name: linear_selector_deep_head
known_encoder_type: linear_selector
use_concept_loss: false

head:
  type: mlp
  tie_weights: false
