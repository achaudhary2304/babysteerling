# Product-key known-concept encoder
# Requires the dataset to have been built with atlas.enable_prototypes=true (concept_prototypes.json).
defaults:
  - base
  - _self_

wandb_name: product_key
known_encoder_type: product_key
